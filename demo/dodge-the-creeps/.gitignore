.zig-cache
zig-out
godot/lib/

# Generated by Godot on import. The `.import` and `.uid` files beside the
# assets are *not* generated in the same sense -- they carry the resource UIDs
# the scenes reference, so they are committed.
**/.godot/**
